# apps/backend

Python 后端、本地服务和任务执行器目录。

职责：

- 提供项目、任务、数据、模型、环境和服务 API。
- 管理本地任务进程、日志、状态、端口和资源检查。
- 执行数据集、模型资产和运行环境检测。
- 为 AI 助手暴露受控工具入口。

初始化前要求：

- 数据库选型未完成前，业务逻辑必须通过 repository / storage 抽象隔离存储实现。
- 高风险动作必须支持用户确认和可追踪日志。
