# 验收记录

## 2026-07-13

- 已实现：首页/右侧 AI 助手模型选择只更新前端当前使用模型，不调用设置默认模型接口，也不调用 `/api/runtime/model`。
- 已实现：模型列表刷新时记录默认模型 ID，只有默认模型 ID 变化时才自动切换当前使用模型。
- 已实现：首页和右侧 AI 助手发送消息时通过 `metadata.modelConfigId` 传递当前使用模型。
- 已实现：设置页改变默认模型后同步全局当前使用模型。
- 已验证：`pnpm run typecheck` 通过。
- 未验证：未启动 Electron 或浏览器执行端到端点击验证。
