# Proposal

## 背景

首页输入区和 AI 对话侧栏共用模型选择工具栏。设置页模型管理已经接入厂商图片，但模型选择入口仍只显示文字，用户在切换模型时缺少直观识别。

## 目标

- 在首页模型选择按钮中显示当前模型对应的厂商图片。
- 在首页和 AI 对话侧栏共用的模型下拉列表中，为每个模型选项左侧显示对应厂商图片。
- 复用已有模型厂商图标映射，未命中时保留文字占位。

## 非目标

- 不新增模型厂商数据结构。
- 不调整模型切换 API。
- 不修改模型管理列表的业务逻辑。
