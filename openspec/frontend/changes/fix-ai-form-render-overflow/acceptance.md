# 验收记录

## 验收项

- “标题 + 多个粗体字段”的 Markdown 文本表单可渲染为只读表单预览。
- 结构化 JSON 表单仍可渲染为只读表单预览。
- AI 回复中的长表单、表格或代码块不再撑开首页对话栏目。
- TypeScript 检查通过。

## 结果

- 已增加 Markdown 文本表单识别：当回复包含标题和多个粗体字段时，渲染为只读表单预览。
- 已保留结构化 JSON 表单预览能力。
- 已限制首页消息列表、消息气泡、Markdown、代码块和表单预览的横向溢出，长内容只在当前栏目内换行或内部滚动。
- `node_modules\.bin\tsc.cmd -b` 通过。

## 未验证

- 未启动 Vite / Electron 做真实对话截图复验。
