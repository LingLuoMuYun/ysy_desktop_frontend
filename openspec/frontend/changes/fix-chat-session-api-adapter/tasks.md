# Tasks

- [x] 检查智能体接口文档和现有前端对话适配。
- [x] 修复会话接口 `ok/sessions/session/deleted` 响应解析。
- [x] 修复流式 `error` 事件处理。
- [x] 补齐编辑最新消息、重新生成、候选回答切换、停止生成的服务层方法。
- [x] 添加最新用户消息编辑并重新生成 UI。
- [x] 添加最新助手回答重新生成 UI。
- [x] 添加候选回答版本切换 UI。
- [x] 将候选回答版本切换优化为左右箭头切换。
- [x] 修复运行模型切换只改前端状态的问题。
- [x] 接入历史会话列表 `/api/sessions`。
- [x] 接入历史会话详情 `/api/sessions/{session_key}` 并恢复消息。
- [x] 接入删除历史会话 `DELETE /api/sessions/{session_key}`，删除前确认。
- [x] 优化删除历史会话确认弹窗 UI，替换浏览器原生确认框。
- [x] 运行前端类型检查。
