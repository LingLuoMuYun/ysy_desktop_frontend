# 模型厂商下拉图标

## 背景

设置页添加模型和编辑模型时，模型厂商下拉菜单当前只展示厂商名称。用户希望在厂商名称左侧展示对应厂商图标，提高识别度。

## 目标

- 复用模型厂商头像资源映射。
- 添加模型和编辑模型的厂商下拉菜单选项左侧展示对应图片。
- 未匹配厂商保留文字兜底标记。

## 非目标

- 不修改模型厂商枚举和后端接口。
- 不改变保存、编辑、连接测试流程。

## 验收标准

- 下拉菜单中已提供图标的厂商左侧显示图片。
- 未匹配厂商不报错并显示兜底标记。
- TypeScript 类型检查通过。
